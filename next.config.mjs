/** @type {import('next').NextConfig} */
const nextConfig = {
  devIndicators: false,
  // Documentation is now served from public/docs/ as static files
  // No rewrites needed - Next.js automatically serves files from public/
};

export default nextConfig;
